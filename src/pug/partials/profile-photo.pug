mixin profile-photo(linkText, linkClass, icon)
  .profile-photo__container
    .profile-photo__image-container(class=linkClass)
      if icon
        .profile-photo__icon
    .block-link.profile-photo__link= linkText
